import {StyleSheet} from 'react-native';
import COLORS from '../../../config/COLORS';

const styles = StyleSheet.create({
    errorText: {
        marginTop: 12,
        color: COLORS.trinidad,
      },
});

export default styles;
